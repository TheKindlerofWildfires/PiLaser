import java.awt.image.BufferedImage;

public class LazerProtocol {

    public BufferedImage processInput(String theInput) {
        //This is a statemachine which returns output based on input
        BufferedImage theOutput = null;
        //Move the raspi based on input
        //Grab image https://github.com/Hopding/JRPiCam
        //Looks like image will be a buffered image
        //return image
        //Don't forget to have an end condition
        return theOutput;
    }
}
